import React from "react";
import { LoaderBox, LoaderText } from "./Loader.styled";

const Loader = () => {
  return (
    <LoaderBox>
      <LoaderText>Loading...</LoaderText>
    </LoaderBox>
  );
};

export default Loader;
