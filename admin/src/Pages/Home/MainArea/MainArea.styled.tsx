import styled from "styled-components";

const MainAreaBox = styled.main`
  flex: 1;
`;
const MainAreaFeedback = styled.section``;
const MainAreaHeader = styled.header``;
const MainAreaTitle = styled.h1``;
export { MainAreaBox, MainAreaFeedback, MainAreaHeader, MainAreaTitle };
